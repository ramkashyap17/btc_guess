export const COUNTDOWN_INTERVAL = 60;
export const USERNAME_KEY_LS = 'username'
export enum UserResponse {
  DOWN = 1,
  UP = 2,
  NONE = 3,
}
export enum UserResult {
  LOSS = 1,
  WIN = 2,
  NONE = 3,
}